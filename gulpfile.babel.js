'use strict';

import gulp from 'gulp';
import requireDir from 'require-dir';

// How to split this out into multiple files
// const tasks = requireDir('./gulp/tasks', { recurse: true });
// Object.keys(tasks).forEach(taskName => {
//     gulp.task(taskName, tasks[taskName]);
// });

// gulp.task('build', gulp.series('clean','sass', 'copy', 'assemble'));
// 
// gulp.task('default', gulp.series('build', done => {
//     console.log("Built dev site");
//     done();
// }));

// Cleaning Steps
// gulp.task('clean:scripts', () => {...})
// gulp.task('clean:styles', () => {...})
// gulp.task('clean:markup', () => {...})
// gulp.task('clean:all', series.parallel('clean:scripts', 'clean:styles', 'clean:markup', () => {...}))

// Scripts

// Styles

// Markup

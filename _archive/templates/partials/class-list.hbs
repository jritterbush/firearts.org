<section class="section" id="class-list">
    <div class="container">
        <div class="row">
            {{#each classes}}
                <div class="col-sm-6 col-md-4">
                    {{> class}}
                </div>
            {{/each}}
        </div>
    </div>
</section>

<section id="video" class="bordered">
    <div class="container">
        <div class="row flex-items-xs-middle">
            <div class="col-xs-6">
                <h2>{{title}}</h2>
                <p>{{{content.copy}}}</p>
                <a href="about.html" class="btn btn-primary">About the Fire Arts Center</a>
            </div>
            <div class="col-xs-6">
                <div class="embed-responsive embed-responsive-16by9">
                    <iframe src="{{content.video.iframe}}"></iframe>
                    <p class="small">{{{content.video.caption}}}</p>
                </div>
            </div>
        </div>
    </div>
</section>
